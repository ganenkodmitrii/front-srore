import { defineStyleConfig } from '@chakra-ui/react'

const Modal = defineStyleConfig({
  baseStyle: {
    closeButton: {
      fontSize: 16,
      color: '#6E7C87',
      top: '16px',
    },
  },
})

export default Modal
